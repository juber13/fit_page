{
  "name": "backend",
  "version": "1.0.0",
  "description": "fit page",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npm install --prefix frontEnd && npm run build --prefix frontEnd"

  },
  "author": "juber",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
